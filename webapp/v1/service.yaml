apiVersion: v1
kind: Service
metadata:
  name: webapp-service-v1
  namespace: webapp
  labels:
    app: employees
    version: v1
spec:
  type: NodePort
  selector:
    app: employees
    version: v1
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30000
